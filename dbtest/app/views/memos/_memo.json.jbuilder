json.extract! memo, :id, :student_name, :student_id, :student_academy, :student_major, :student_degree, :student_credit, :student_course, :student_password, :created_at, :updated_at
json.url memo_url(memo, format: :json)
